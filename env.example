# Copy this file to .env for local development and fill in your real key.
# Never commit the actual .env file — only this example.

ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional — defaults to 3001 if unset. Render sets this automatically
# in production, so you don't need to set it there.
PORT=3001
